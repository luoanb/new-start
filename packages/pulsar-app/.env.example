# 前端 dev 端口 = DEV_FRONT_PORT（默认取 tauri.conf.json 的 devUrl,1432），后端 dev 端口 = PULSAR_PORT（默认 8899），
# 用 `pnpm tauri:dev --frontend-port .. --backend-port ..` 临时指定，无需在本文件配置。
# 可选的 dev 后端代理显式覆盖（默认 http://127.0.0.1:PULSAR_PORT）：
#DEV_PROXY_TARGET=http://127.0.0.1:8899