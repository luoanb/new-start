# create_directory

## 工具

在活动工作区内创建目录（自动创建缺失的父目录）。

参数：

- `path`（必填）：相对活动工作区根路径的目录路径。

若目标路径已有文件则报错。无返回值。

## 对模型的期待

- 需要先建目录再放文件、或组织项目结构时调用。
- 若目录已存在，会静默成功（幂等），无需重复创建。

## 忌用

- 不要在工作区之外创建目录（会被拒绝）。
- 目标路径上是文件时不要重试同一路径，先 `file_info` 确认。

## 注意

- 只作用于当前 active 工作区。
