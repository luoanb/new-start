# select_neuron_candidates

## 工具

选取高权重的神经元候选（必要时补建缺失候选），返回候选神经元列表。

调用时传入 JSON 参数：

```json
{"n": 5, "source_id": "可选来源神经元", "min_new": 0}
```

## 对模型的期待

- `n` 为必填，表示要取的候选数量；按实际需要取小值。
- `source_id` 可选，限定在指定来源的邻域内选；`min_new` 默认 0，表示至少补建的候选数。

## 忌用

- 不要传入过大的 `n`——结果集会进入上下文，大 `n` 会撑爆上下文。
