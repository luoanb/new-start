# 前端 dev 端口由 DEV_FRONT_PORT 控制、后端由 PULSAR_PORT 控制（见 scripts/dev-tauri.mjs / vite.config.js），
# 均可用 `pnpm tauri:dev --frontend-port .. --backend-port ..` 临时指定；本文件不再硬编码端口。
# DEV_PROXY_TARGET 仅用于显式覆盖 dev 后端代理地址（默认 http://127.0.0.1:PULSAR_PORT）。
#DEV_PROXY_TARGET=http://127.0.0.1:8899