# Product

## Register

product

## Users

混合用途用户群体，按使用场景分为两类：

1. **Agent 开发者/技术用户** — 在 Agent 模式下调试工具调用行为，在 Assistant 模式下观察 Neuron 自主推进；关注消息流、工具执行、会话状态等工程细节。
2. **日常 AI 助手用户** — 在 Chat 模式下进行日常对话、信息检索、任务辅助；关注对话流畅性、响应速度和交互直觉。

两种身份通过会话模式（Chat / Agent / Assistant）自然切换，UI 需要在同一界面中兼顾两个群体的需求。

## Product Purpose

一个功能完整的 AI Agent 桌面客户端，提供三种对话模式：
- **Chat**: 普通对话，一问一答，不调用工具，无自主推进
- **Agent**: 可调用工具的对话，Engine 按需执行 tool-calling 循环
- **Assistant**: 自主推进模式，Poller 按 tick 调度，Neuron 驱动课题深入

核心目标是让 GUI 在功能覆盖度上追上 TUI，并在 Web 原生交互（表单、列表、弹窗、视觉层级）上超越终端体验。

## Brand Personality

简洁、流畅、可靠。

- **简洁**: 不堆砌元素，每个 UI 组件都有明确的存在理由
- **流畅**: 过渡自然、反馈即时、操作路径短
- **可靠**: 信息呈现清晰，错误状态温和且有帮助

## Anti-references

- **不花哨不炫技**: 无玻璃拟态、无渐变文字、无装饰性动画。视觉效果服务功能清晰度，不从形式出发。
- **不暗黑千篇一律**: 不做默认深色主题的工具感 UI。主题适配系统偏好，但无论深色/浅色都有独立的视觉辨识度。
- **不拥挤不杂乱**: 信息密度适中，保留呼吸感。不塞满屏幕，不做 SaaS 后台模板式的卡片堆叠。
- **不套 AI 对话产品模板**: 参考 ChatGPT/Claude 的对话沉浸感，但不复制其视觉细节。要有自己的视觉语言。

## Design Principles

1. **Chat-first, not dashboard** — 对话是核心交互范式。所有 UI 围绕聊天流展开，会话列表、模型选择、信息面板都是聊天工作台的支撑，而非独立功能模块。

2. **呼吸感优于信息密度** — 宁可滚动也不压缩。留白是设计工具，间距传达层级，不将屏幕空间视为需要填满的资源。

3. **模式可见即所得** — Chat/Agent/Assistant 三种模式在 UI 中清晰可辨。用户无需猜测当前处于什么状态，模式标签、状态栏、输入区行为三者一致。

4. **克制即优雅** — 不添加装饰。边框、背景、色彩都承担语义角色。如果某个视觉元素没有功能目的，删除它。

5. **反馈先于形式** — 加载状态、错误提示、操作结果比视觉风格更重要。任何用户操作必须在 100ms 内给予视觉反馈。

## Accessibility & Inclusion

基础可用级别：
- 色彩对比度满足基本可读性（不严格要求 WCAG AA，但避免低对比度文本）
- 键盘导航支持 Tab 切换焦点区域和快捷键操作
- 支持系统深色/浅色模式自动适配
- 不依赖颜色作为唯一的信息传递手段
